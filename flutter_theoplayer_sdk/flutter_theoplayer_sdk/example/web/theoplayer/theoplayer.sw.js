/*
 * The Service Worker based HESP playback pipeline for iOS Safari has been removed from the THEOplayer SDK.
 * HESP playback on iOS Safari now uses the ManagedMediaSource API, which is available on iOS 17.1 and higher.
 *
 * This file is intentionally left empty. It is still shipped so that existing build pipelines and pages that
 * copy or register `theoplayer.sw.js` keep working. It is safe to stop serving this file.
 */
